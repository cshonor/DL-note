# 《深度学习图解》第4章 · 梯度下降在对谁求导？

> 上接 **`01_预测比较学习与梯度下降链路.md`**；损失形式与书中平方误差见 **`02_误差与均方误差.md`**。本篇把「梯度到底是对谁求导」一句话说死，写出单参数线性模型下的链式拆开，并收入 **4.11** 一带的**代码—几何（`img/8.png`）—手算**与书中 **`input=1.1` 四轮例（`img/9.png`、`img/10.png`）**（不用 LaTeX 环境）。

---

## 一、一句话答案（记这个）

**梯度下降，是对损失函数 Loss 求导**——更准确地说，是对**每个待学习参数**求 **Loss 对该参数的偏导数**，例如：

- **∂Loss / ∂w**（权重 w）
- **∂Loss / ∂b**（偏置 b，若模型里有 b）

更新权重时用的就是这一组数（再乘学习率 η，沿**降低 Loss** 的方向走一小步）。

---

## 二、为什么是对 Loss 求导？

目标只有一个：**让 Loss 尽量小**。

**梯度（对某个 w 的偏导）**回答的是：

> 若只把 **w** 动一点点，**Loss** 会**往哪个方向、大约变多少**？

- 偏导数为**正**：w 略增大 → Loss 增大 → 更新时应**减小 w**（沿负梯度走）。  
- 偏导数为**负**：w 略增大 → Loss 减小 → 更新时应**增大 w**。

所以：**「对 Loss 求关于 w 的偏导」**就是更新 **w** 的依据；对 **b** 同理。

---

## 三、4.11 梯度本质：极简代码、碗形曲线（配图）与手算

对应书中 **4.11** 前后把「训练在干什么」收束到一条线：**固定样本时，Loss 是权重 w 上的碗；切线斜率即梯度；五代码行即一轮梯度下降骨架**。

### 1. 极简训练循环逐行对应什么

```python
pred = input * weight                  # 1. 前向：由当前 w 得到预测
error = (pred - goal_pred) ** 2        # 2. 比较：平方误差（单样本时常直接打印为「误差」）
delta = pred - goal_pred               # 3. 原始差（书中常叫 delta / 节点 delta）
weight_delta = delta * input           # 4. 与 ∂Loss/∂w 同型的一项（见下「½」约定）
weight = weight - weight_delta         # 5. 学习：沿其反方向走一步（等价 w ← w − η·∂Loss/∂w 的特例）
```

把 **2～5** 放进 `for` 里反复做，就是**最小版「预测 → 损失 → 梯度信息 → 更新」**。实战里常在 **5** 上乘学习率 **`alpha`**（**`η`**），并做批量/正则等，但**骨架**不变。

### 2. 相对冷热学习，这段代码在干什么

1. **不再**左右试探：用 **`delta * input`** 一次给出「该往哪边、动多少」的一阶信息。  
2. **`w ← w − weight_delta`** 与 **`w ← w − η · ∂Loss/∂w`** 同族：都是**沿降低 Loss 的方向**动 **w**（符号由 **`delta`** 与 **`input`** 共同决定，见下文**第十二、十三节**）。  
3. 迭代足够多次，**w** 会逼近使 **`pred ≈ goal`** 的值（单样本、严格凸抛物线时，碗底即 **Loss** 最小）。

### 3. 核心几何：固定 x、y 时，Loss(w) 是「碗」形抛物线（**`img/8.png`**）

令 **`pred = x·w`**，**`y = goal_pred`**。与 **`02`** 打印一致的平方误差（不写 ½）：

**Loss(w) = (x·w − y)²**

例：**`x = 0.5`**，**`y = 0.8`**，则 **Loss(w) = (0.5·w − 0.8)²**：横轴 **w**、纵轴 **Loss**，为**开口向上**的抛物线；**全局最低点**在 **`0.5·w = 0.8`** 即 **`w* = 1.6`**（此时 **Loss = 0**）。曲线上每一点的**切线斜率**就是 **dLoss/dw**（一元里即梯度）。下面即**本书该段的配图**（权重–误差与「斜率」标注）：

![横轴权重、纵轴误差；黑点为当前位置，切线标注斜率，谷底为误差最小附近 — 4.11 碗形曲线配图](img/8.png)

### 4. 斜率与代码行（务必对齐「½」）

1. **切线斜率** = **dLoss/dw** = 梯度（标量 **w** 时常不再区分「偏导」说法）。  
2. 若采用 **`Loss = ½·(pred − y)²`**（与下文**第五节**同一约定），则 **∂Loss/∂w = (pred − y)·x = delta * input**，与 **`weight_delta = delta * input`** **完全一致**；此时 **`weight = weight - weight_delta`** 即 **`η = 1`** 的一步。  
3. 若 **`Loss = (pred − y)²`（无 ½）**，则 **∂Loss/∂w = 2·(pred − y)·x**；仍写 **`weight_delta = delta * input`** 时相当于少乘 **2**，可用**更小步长**或显式 **`w -= eta * 2 * ...`** 吸收（下文**第五节**末：常数只重标定 **η**）。  
4. **符号**：**dLoss/dw > 0** → 应**减小 w**；**< 0** → 应**增大 w**；**= 0** → 一阶驻点（此抛物线即碗底）。

「变化率里为什么能读方向」：**一元导数的正负**回答「**w** 往右微挪时 **Loss** 升还是降」，与更新方向一一对应；**绝对值**反映坡陡缓。

### 5. 与冷热学习划界

|  | 冷热学习 | 梯度下降（上段五代码骨架） |
|--|----------|---------------------------|
| 方向 | 试 **w±步长** 比 **Loss** | 用 **dLoss/dw**（或与其成比例的 **`weight_delta`**） |
| 步长 | 试探步与更新步绑死 | 可用 **`alpha`** 单独调；与梯度模长共同决定一步远近 |
| 前向次数 | 每步多次预测 | 通常每步一次 **pred** 即可算 **delta** |

### 6. 一句话

**入门标量版**：在 **Loss(w)** 这条「碗形」曲线上，用**梯度（切线斜率）**决定往哪挪 **w**；**`delta * input`** 在 **½** 损失约定下就是 **∂Loss/∂w**，与书中最简写法逐行对齐。

### 7. 手算四轮（`input = 0.5`，`goal_pred = 0.8`，初值 `w = 0.5`）

以下按 **`weight_delta = delta * input`** 且 **`w ← w − weight_delta`**，等价 **`Loss = ½(pred−y)²`**、**`η = 1`**（与 **`03`** 第六节 **`½`** 手算一步同一约定）。**`error = (pred−y)²`** 仍用**无 ½** 的平方，便于和 **`02`** 的 **0.3025** 对照。

| 轮次 | w | pred = 0.5·w | delta = pred−0.8 | weight_delta = delta·0.5 | 下一轮 w | error = (pred−0.8)² |
|------|---|----------------|-------------------|---------------------------|----------|---------------------|
| 0 | 0.500000 | 0.250000 | −0.550000 | −0.275000 | 0.775000 | 0.302500 |
| 1 | 0.775000 | 0.387500 | −0.412500 | −0.206250 | 0.981250 | 0.170156 |
| 2 | 0.981250 | 0.490625 | −0.309375 | −0.154688 | 1.135938 | 0.095713 |
| 3 | 1.135938 | 0.567969 | −0.232031 | −0.116016 | 1.251953 | 0.053839 |

四轮后 **w ≈ 1.252**，仍在最优 **w* = 1.6** 左侧；继续迭代沿抛物线向碗底靠近。若改为 **`w -= alpha * weight_delta`**（如 **`alpha = 0.01`**），每步更小，需更多轮，但仍是同一梯度乘学习率。

### 8. 书中另一例：`input = 1.1`，`goal_pred = 0.8`，初值 `weight = 0`（四轮与「过碗底」）

书中用下面循环（**`range(4)`**）演示**同一条五代码行**，但换了一组数：**`input` 较大**、**`weight` 从零起步**，第一步 **`weight_delta = delta * input`** 的绝对值会很大，**黑点会在误差碗上「一大步」跨到最优权重另一侧**，再靠后续几步把 **`pred`** 拉回 **`goal_pred`** 附近。最优仍满足 **`1.1·w* = 0.8`**，即 **`w* ≈ 0.727`**。

```python
weight, goal_pred, input = (0.0, 0.8, 1.1)

for iteration in range(4):
    print("-----\nWeight:" + str(weight))
    pred = input * weight
    error = (pred - goal_pred) ** 2
    delta = pred - goal_pred
    weight_delta = delta * input
    weight = weight - weight_delta
    print("Error:" + str(error) + " Prediction:" + str(pred))
    print("Delta:" + str(delta) + " Weight Delta:" + str(weight_delta))
```

**读图要点**（与上文「3. 核心几何」及 **`img/8.png`** 同一条碗形线）：

1. **第 1 轮**：**`w = 0`** 在碗**左侧高处**，切线斜率为**负**，**`delta = −0.8`**，**`weight_delta = −0.88`**，**`w ← w − weight_delta`** 等价**大幅增大 w**（向碗底方向走），**`error = 0.64`**。  
2. **第 2 轮**：**`w`** 已到 **≈0.88**，已超过 **w\***，点在碗**右侧**，斜率变**正**，下一步会**减小 w**（书中口语：步子迈大、从另一侧退一步）。**`error`** 已骤降到约 **0.028**。  
3. **第 3、4 轮**：在谷底两侧微调，**`|weight_delta|`** 随 **`|delta|`** 变小，切线趋平，**`pred`** 逼近 **0.8**，**`error`** 可到 **1e−4～1e−7** 量级（与书页打印一致）。

图 1：前两轮数值流 + 误差–权重曲线上的点与切线。

![书中例：第1–2轮，权重大幅增加与过冲后回退（示意）](img/9.png)

图 2：第 3–4 轮靠近谷底 + 四轮 `print` 汇总。

![书中例：第3–4轮与控制台输出](img/10.png)

---

## 四、完整链路（链式法则）

不是「跳过 Loss 直接对模型瞎求导」，而是：

1. **模型（预测）**：例如单输入线性 **`ŷ = w·x + b`**（书中常从更简单 **`ŷ = w·x`** 起步，b 可后加）。  
2. **损失**：例如均方误差一种常见写法 **`Loss = ½·(ŷ − y_true)²`**（与 `02` 里 **`(pred − goal)²`** 差常数 **½**，只影响梯度整体倍数，**不改变「对谁求导」**的结论）。  
3. **要的量**：**∂Loss / ∂w**、**∂Loss / ∂b**。  
4. **链式**：**∂Loss / ∂w = (∂Loss / ∂ŷ) · (∂ŷ / ∂w)**（中间经过预测 **ŷ**）。

---

## 五、拆开算（以 Loss = ½(ŷ − y_true)²，ŷ = w·x + b 为例）

**① ∂Loss / ∂ŷ**

- **Loss = ½·(ŷ − y_true)²**  
- **∂Loss / ∂ŷ = ŷ − y_true**

**② ∂ŷ / ∂w、∂ŷ / ∂b**

- **ŷ = w·x + b**  
- **∂ŷ / ∂w = x**（此时把 x、b 当常数）  
- **∂ŷ / ∂b = 1**

**③ 合成**

- **∂Loss / ∂w = (ŷ − y_true) · x**  
- **∂Loss / ∂b = (ŷ − y_true) · 1 = ŷ − y_true**

若损失写成 **`Loss = (ŷ − y_true)²`（没有前面的 ½）**，则 **∂Loss / ∂ŷ = 2·(ŷ − y_true)**，进而 **∂Loss / ∂w** 会多一个因子 **2**；更新时把 **η** 调一下即可吸收，**求导对象仍是 Loss**。

---

## 六、「对 Loss 求导」还是「对模型求导」？

- **最终落点**：都是 **Loss 对每个参数的偏导**（**∂Loss/∂w**、**∂Loss/∂b**）。  
- **中间**：要经过 **ŷ = f(x; w, b)**，所以用**链式法则**把 **∂Loss/∂ŷ** 与 **∂ŷ/∂w** 乘起来。  
- **不要记成「对模型求导」与「对 Loss 求导」二选一**；标准说法是：**对参数求导，Loss 在链子的最外一层。**

---

## 七、多个权重时：其余参数当常数

求 **∂Loss / ∂w₂** 时，**w₁、w₃、b** 等在求偏导过程中都**视为常数**；得到的是 **「w₂ 单独动一下，Loss 怎么变」**，即 **w₂ 的专属梯度**。

---

## 八、更新公式（写法对齐）

**w ← w − η · (∂Loss / ∂w)**  

**b ← b − η · (∂Loss / ∂b)**  

**η** 为学习率（步长）。与「导数为正则要减小 w」的叙述一致：减去正梯度，w 减小，Loss 往下降方向走（一维凸情形下的直观；实际曲面更复杂时再谈学习率与收敛）。

---

## 九、书中一例：同一条流水线上「一次」模拟更新

下面四张图是**同一轮迭代**的切片（不是四件无关的事）：固定一组 **`(input, goal_pred)`**（脚趾 **8.5**，胜负标签 **1**），从初始 **`weight`** 出发，依次 **预测 → 平方误差与 delta → `weight_delta` → 用 `alpha` 写回权重**。把 **`delta`** 看成 **`(pred − goal)`**，**`weight_delta = input * delta`** 与 **`∂Loss/∂w = (pred−y)·x`** 同型；再乘 **`alpha`**，就与**第八节** **`w ← w − η·(∂Loss/∂w)`** 里学习率 **`η`** 的角色一致（书中 **`weight -= weight_delta * alpha`** 即沿 **`weight_delta`** 的反方向迈 **`alpha`** 倍步长）。

**① 初始网络与 `neural_network`**

![单输入单权重：脚趾数目 → 是否获胜；weight=0.1、alpha=0.01](img/4.png)

**② 预测、平方误差，以及 `delta = pred - goal_pred`**

![8.5×0.1=0.85，MSE 与原始差、平方](img/5.png)

**③ `weight_delta = input * delta`（权重增量 / 与梯度同型）**

![权重增量与更新方向](img/6.png)

**④ `weight -= weight_delta * alpha`（新权重示意）**

![alpha 为训练前取定的步长；旋钮上为新权重约 0.1125](img/7.png)

---

## 十、极简表

| 问题 | 答案 |
|------|------|
| 梯度下降「对谁」求导？ | **对 Loss**，对每个要学的 **w、b** 求 **∂Loss/∂参数** |
| 为什么出现链式？ | **Loss** 通过 **ŷ** 依赖 **w、b**，要乘 **∂Loss/∂ŷ** 与 **∂ŷ/∂w** 等 |
| 更新依据 | **负梯度方向** × **η** |

---

## 十一、带数字的完整例题（两套损失）

平方误差（含「梯度为 0」与「一步更新 w」）与对数损失 + sigmoid 的逐步数值，见 **`05_平方误差与对数损失_数值例题.md`**。

---

## 十二、为什么说「方向」？——符号与变化率

对单个标量 **w**，**g = ∂Loss/∂w** 这一个数里同时装了两件事：

- **符号（正 / 负）**：**w** 该**增大还是减小**，才能让 **Loss** **局部**下降（一维里口语说的「方向」几乎就是它）。  
- **绝对值 |g|**：**坡有多陡**、**w 微动一下 Loss 有多敏感**（变化率的大小）。  
- **g = 0**：局部上 **w** 怎么微动 **Loss** 一阶不变（临界点；是否全局最优另说）。

这与 **第二节** 的叙述是同一件事：**正梯度 → 更新要减小 w**；**负梯度 → 更新要增大 w**。这里再钉一句：**「方向」在一维里主要就是导数的正负**，不是另起炉灶的概念。

---

## 十三、再用 pred = w·x 看一遍符号（与第五节一致）

**Loss = ½·(pred − y)²** 时 **g = ∂Loss/∂w = (pred − y)·x**。没有 **½** 时 **g** 多因子 **2**，**符号不变**。

### A. g > 0

**w** 略增 → **Loss** 增；**w** 略减 → **Loss** 减 → 应**减小 w**。**w ← w − ηg** 在 **g>0** 时让 **w** 变小。

### B. g < 0

**w** 略增 → **Loss** 减 → 应**增大 w**。**g<0** 时 **−ηg>0**，**w** 变大。

### C. g = 0

本步可不更新 **w**（一阶上已不敏感）。

---

## 十四、多维与冷热学习

- **多维**：各 **∂Loss/∂wᵢ** 排成向量 **g**；**−g** 是参数空间里**一步最陡下降**的方向；**每个分量的符号**仍表示该坐标该增还是该减。  
- **冷热学习**：试 **w+步长**、**w−步长** 看 **Loss** 谁小，是在**试往哪边挪**；**梯度法**用 **g** 的符号**直接读出**往哪边挪，一般**不必每步试两次**。

---

## 十五、三个具体数（一眼看符号）

仍用 **pred = w·x**，**Loss = ½(pred−y)²**，**g = (pred−y)·x**。

| 情形 | x | y | w | pred | g = (pred−y)·x | 为降 Loss 对 w 的直觉 | w←w−ηg（η>0） |
|------|---|---|---|------|----------------|----------------------|----------------|
| 1 | 0.5 | 0.8 | 0.5 | 0.25 | **−0.275** | 增大 w | w 增大 |
| 2 | 0.5 | 0.1 | 0.5 | 0.25 | **+0.075** | 减小 w | w 减小 |
| 3 | 0.5 | 1.0 | 2.0 | 1.0 | **0** | 一阶不动 | 本步不改 |

第 1 行与 **`02`** 中一步更新 **w** 的直觉一致（pred 低于目标，**g<0**，更新增大 **w**）。
