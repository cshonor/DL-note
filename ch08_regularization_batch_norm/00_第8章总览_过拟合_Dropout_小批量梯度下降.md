# 第8章：过拟合、Dropout 与小批量梯度下降
**Core Concept: Overfitting -> Regularization with Dropout -> Stable Training with Mini-batch**

## 一、整章一句话
第8章就是训练实战三连：  
**模型为什么会学太死（过拟合） -> 怎么压住（Dropout） -> 怎么训得又快又稳（Mini-batch）。**

---

## 二、本章只讲三件关键事

### 1) 过拟合（Overfitting）
- **人话解释**：模型把训练集里的噪声和巧合也记住了。
- **本质**：训练表现很好，但泛化能力变差。
- **典型表现**：训练准确率高，测试准确率低；训练 loss 降，验证 loss 反升。

一句话：  
**不是模型不会学，而是学得太死。**

---

### 2) Dropout（经典防过拟合手段）
- **做法**：训练阶段随机关闭一部分神经元。
- **目的**：防止模型过度依赖少数节点，减少“背答案”行为。
- **效果**：特征学习更分散、更稳健，泛化通常更好。
- **使用规则**：训练时开 Dropout，推理时关 Dropout。

工程直觉：  
**Dropout 相当于强制模型别走单一路径，逼它学可迁移的规律。**

---

### 3) 批量梯度下降方式（Batch / SGD / Mini-batch）
核心问题是：**一次用多少样本更新权重**。

- **Batch Gradient Descent**
  - 用全量训练集算一次梯度再更新
  - 梯度稳定，但速度慢、资源压力大

- **SGD（随机梯度下降）**
  - 每次只用 1 个样本更新
  - 更新频繁、速度快，但抖动大

- **Mini-batch Gradient Descent（主流）**
  - 每次用一小批样本（常见 32 / 64 / 128）
  - 在稳定性、速度、显存占用之间取得最好平衡

本章实践结论：  
**小批量训练是现代深度学习默认方案。**

---

## 三、专题延伸（按需阅读）
- `01_Dropout与固定冻结的区别.md`：专门讲清“固定冻结不推荐、Dropout 推荐”的根本原因。
- `02_L1与L2正则化_为什么都能把权重往0拉.md`：用更新公式和梯度视角讲透 L1/L2 的作用机制与差异。

---

## 四、和第7章的关系（承接）
- 第7章（你当前这条线）侧重：可视化、关联抽象、变量表达。
- 第8章侧重：训练时会遇到的真实问题与解决策略。

可以理解为：
- 第7章回答“怎么看懂网络在表达什么”
- 第8章回答“怎么让网络在真实训练里学得对、学得稳”

---

## 五、本章核心结论（收束）
- 过拟合是训练实战中最常见风险之一。
- Dropout 是简单有效、成本低的防过拟合工具。
- Mini-batch 是训练效率和稳定性的工程最优折中方案。
- 这三者合在一起，构成“能训、稳训、泛化不崩”的基础框架。
